# QRM-UI Future Tasks
*   **Real Chord List Builder:** Replace the simple text inputs for Chords with a robust, visual chord list builder.
*   **Implement `restPct` & `tonicPct` as Groove Presets:** Instead of building a complex step-sequencer UI for per-beat arrays, use a `Choice` dropdown to provide predefined probabilistic patterns (e.g., "Flat 50%" -> `0.5`, "Heavy Downbeat" -> `[0.8, 0.2, 0.8, 0.2]`). This preserves the engine's power while keeping the UI clean and high-level.
*   **Durations as Weighted Arrays:** Support duration inputs as weighted arrays (e.g., "1/8b", "1/4a" instead of just basic fractions).

*Note: This file was created to reliably resume context for future sessions.*
