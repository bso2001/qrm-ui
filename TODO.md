# QRM-UI Future Tasks
*   **Real Chord List Builder:** Replace the simple text inputs for Chords with a robust, visual chord list builder.
*   **Implement `restPct`:** Ensure the UI and generator properly support rest percentage parameters.
*   **Implement `tonicPct`:** Ensure the UI and generator properly support tonic percentage parameters.
*   **Durations as Weighted Arrays:** Support duration inputs as weighted arrays (e.g., "1/8b", "1/4a" instead of just basic fractions).

*Note: This file was created to reliably resume context for future sessions.*
